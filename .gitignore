# Binaries and build output
/bin/
*.exe
*.dll
*.so
*.dylib

# Test and coverage output
*.test
*.out
coverage.*

# Local configuration and secrets — never commit these
config.yaml
config.local.yaml
*.pem
*.key
!*.example.yaml

# Editor / OS noise
.DS_Store
.idea/
.vscode/
*.swp

# Generated per run by deploy/gen-material.sh; never committed. The fixtures
# name keys by fingerprint, so they cannot exist before the keys do.
deploy/keys/
deploy/bin/
deploy/control/fixtures.yaml
